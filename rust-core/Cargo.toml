[package]
name = "mechbox-core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2", features = ["async"] }
napi-derive = "2"
rand = "0.8"
rand_distr = "0.4"

[build-dependencies]
napi-build = "2"

[profile.release]
lto = true
codegen-units = 1
